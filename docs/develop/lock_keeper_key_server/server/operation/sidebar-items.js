window.SIDEBAR_ITEMS = {"fn":[["audit_event","Log the given action as an audit event."],["handle_authenticated_request","Takes a request from `tonic` and spawns a new thread to process that request through the logic defined by the `Operation::operation` method. Any errors returned by the operation are logged and an appropriate error message is sent to the client."],["handle_error",""],["handle_unauthenticated_request","Takes a request from `tonic` and spawns a new thread to process that request through the logic defined by the `Operation::operation` method. Any errors returned by the operation are logged and an appropriate error message is sent to the client."]],"trait":[["Operation","A type implementing [`Operation`] can process `tonic` requests using a message-passing protocol facilitated by a [`Channel`]."]]};