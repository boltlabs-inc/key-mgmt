window.SIDEBAR_ITEMS = {"struct":[["LockKeeperClient","A `LockKeeperClient` is an abstraction over client operations; that is, it wraps around the state and infrastructure necessary to make requests to the key server. It handles confidentiality, integrity, and authentication of communication with the server. A `LockKeeperClient` exists for the duration of one authenticated session, during which multiple requests can be made to the server."],["Password",""]]};